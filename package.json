{
  "name": "thump",
  "version": "1.0.0",
  "private": true,
  "description": "Browser drum machine with Rust backend",
  "scripts": {
    "check": "node --check core.js && node --check app.js",
    "test": "node --test tests/*.test.js",
    "test:ui": "node tests/smoke.js"
  },
  "devDependencies": {
    "jsdom": "^24.1.3"
  }
}
